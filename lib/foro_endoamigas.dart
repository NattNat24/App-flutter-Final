import 'package:flutter/material.dart';
class ForoEndoAmigasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Foro EndoAmigas'), backgroundColor: Color(0xFFEC407A)),
      body: Center(
        child: Text('Comparte y conecta con EndoAmigas'),
      ),
    );
  }
}
